// This function ia to test if the button is being interacted with

function goto(){
    //This is just to check if the button is being interacted with
    //alert("Button clicked");
        
  }

  //This function changes the text when the mouse hovers over the buttons
  function mOver(obj) {
    obj.innerHTML = "Thank You"
  }
  
  function mOut(obj) {
    obj.innerHTML = "Mouse Over Me"
  }

  